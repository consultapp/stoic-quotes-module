{
  "name": "stoic-quotes-module",
  "version": "1.0.0",
  "description": "A web page module that displays Stoic quotes.",
  "main": "index.js",
  "scripts": {
    "watch:css": "sass --watch src/:dist/css",
    "dev": "tsc -b && sass src/:dist/css ",
    "build": "tsc -b && sass src/:dist/css ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "target": "es2017",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "scss": "^0.2.4",
    "typescript": "^5.6.3"
  }
}
